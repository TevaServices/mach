# Only the repo owner is listed, deliberately.
#
# GitHub auto-requests review from every code owner the moment a PR opens,
# and CODEOWNERS has no way to opt one person out of that. Listing a second
# maintainer here pings them on every PR targeting `main` — do not add one.
# The `Main` ruleset's code-owner requirement is satisfied by the single
# entry below.
* @bcross
